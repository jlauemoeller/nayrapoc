# Running Infrastructure Components Locally

You can launch pre-configured versions of some of our infrastructure components
using the Docker Compose file in this directory:

> docker compose build # build containers
> docker compose up # start all containers
> docker compose down # stop all containers
